/**
 * Stable Prisma re-export for Nest + IDE.
 * Use the generated client from `@prisma/client`.
 */
export {
  Prisma,
  PrismaClient,
  Role,
  RequirementStatus,
  $Enums,
} from '@prisma/client';
